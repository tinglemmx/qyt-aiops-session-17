input {
  beats {
    port => 5044
  }
}

filter {
  # grok 按进程和时间解析 VyOS 日志
  grok {
    match => { "message" => "%{SYSLOGTIMESTAMP:timestamp} %{HOSTNAME:host} %{DATA:process}(?:\[%{NUMBER:pid}\])?: %{GREEDYDATA:msg}" }
  }

  # 可选：根据 process 分类不同类型日志
  if [process] =~ "ospfd" {
    mutate { add_field => { "log_type" => "ospf" } }
  } else if [process] =~ "Keepalived_vrrp" {
    mutate { add_field => { "log_type" => "vrrp" } }
  } else if [process] =~ "openvpn" {
    mutate { add_field => { "log_type" => "vpn" } }
  } else {
    mutate { add_field => { "log_type" => "other" } }
  }

  date {
    match => [ "timestamp", "MMM  d HH:mm:ss", "MMM dd HH:mm:ss" ]
    timezone => "Asia/Shanghai"
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "vyos-logs-%{+YYYY.MM.dd}"
  }

  stdout { codec => rubydebug }
}
